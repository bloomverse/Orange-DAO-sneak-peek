using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace TPSBR
{
    public class DummyTransform : MonoBehaviour
    {        
        // Update is called once per frame
        void Update()
        {
            
/*            if(this.GetComponentInParent<Weapons>()!=null && this.GetComponentInParent<Weapons>().CurrentWeapon.GetComponent<HitscanWeapon>())
            {
                //this.transform.position = this.GetComponentInParent<Weapons>().CurrentWeapon.GetComponent<HitscanWeapon>()._fireTransform.position;
                //this.transform.rotation = this.GetComponentInParent<Weapons>().CurrentWeapon.GetComponent<HitscanWeapon>()._fireTransform.rotation;
            }*/
            
        }
    }
}
